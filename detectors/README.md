The detectors directory contains all the individual microservices used for deepfake detection. Each subfolder represents a specific detector microservice, such as video classification or rPPG-based heartbeat consistency. These detectors are designed to be independently containerized for scalability. Moreover, each service exposes a schema-conformant `/detect` endpoint for standardized communication with the rest of the system.
