import { DetectorResultCardDemo } from "../../components/DetectorResultCard";

export const metadata = {
  title: "DetectorResultCard Preview — AEGIS",
  description:
    "Standalone visual verification page for the DetectorResultCard component — all states rendered simultaneously.",
};

export default function DemoPage() {
  return <DetectorResultCardDemo />;
}
