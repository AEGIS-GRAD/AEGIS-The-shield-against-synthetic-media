This folder contains the hardware-aware inference engine for AEGIS. It handles critical deployment optimization tasks such as model quantization, pruning, and ONNX or TensorRT export. Furthermore, it manages telemetry reporting to ensure efficient operation during edge deployment. This engine ensures that the deepfake detection models run as fast and efficiently as possible on target hardware.
